# REIS backend development dependencies
#
# Install after requirements.txt when you need tests, linting, notebooks,
# or optional experiment tracking:
#
#   python -m pip install -r backend/requirements-dev.txt

-r requirements.txt

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==4.1.0

# Linting and type checks
ruff==0.2.2
mypy==1.9.0

# Notebooks and experiment tracking
notebook==7.5.5
jupyterlab==4.5.6
mlflow==2.13.0
